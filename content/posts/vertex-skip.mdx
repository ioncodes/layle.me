---
author: "Layle"
title: "vertex-skip: The Case of Luigi's Extra Triangle"
summary: "A stray triangle stretched across Mario Golf's menu because Gecko drew a vertex the game had told it to skip."
image:
  alt: "An extra triangle stretches from Luigi's hand in Mario Golf's character selection menu."
  src: "/images/gecko/vertex-skip-before.png"
  position: "40% 50%"
tags: ["emulation", "gamecube", "wii", "graphics", "gecko"]
date: 2026-09-17T18:05:00Z
sticky: true
unlisted: true
---

import ImageComparison from '../../src/components/posts/ImageComparison.astro';
import VertexSkipDemo from '../../src/components/posts/VertexSkipDemo.astro';
import VertexRecords from '../../src/components/posts/VertexRecords.astro';
import TestPanels from '../../src/components/posts/TestPanels.astro';

While testing *Mario Golf: Toadstool Tour* in [Gecko](https://github.com/ioncodes/gecko), my GameCube and Wii emulator, I noticed a thin triangle stretching from Luigi's hand across the character selection menu.

Look between Luigi's hand and Wario's portrait:

<ImageComparison
  before="/images/gecko/vertex-skip-before.png"
  after="/images/gecko/vertex-skip-after.png"
  beforeAlt="Mario Golf before the fix: a thin diagonal triangle stretches from Luigi's hand across the menu."
  afterAlt="The same character selection frame after the fix, without the stray triangle."
  zoom={{ label: 'Enlarge the spike area', origin: '30% 60%' }}
/>

The cause was a position index meaning **skip this vertex**, which Gecko treated as an ordinary array entry and turned into an extra point and triangle.

## A bit of geometry

The GameCube's graphics processor, [GX](https://www.gc-forever.com/wiki/index.php?title=Hardware_Acronyms#GP,_GX), fills triangles built from **vertices**: corners with positions and attributes such as color.

In a [triangle strip](https://wikis.khronos.org/opengl/Primitive#Triangle_primitives), the first three vertices make a triangle and each additional vertex makes another triangle with the previous two corners. Number a rectangle's corners like this:

```text
0 ------ 2
|        |
|        |
1 ------ 3
```

Sending `0, 1, 2, 3` makes triangles `0, 1, 2` and `2, 1, 3`, which share the diagonal between corners 1 and 2. Swapping the first two vertices of every other triangle preserves their **winding order**, so both face the same way.

Games can send coordinates directly or use **indices** into a position array, where index `2` means "use position number 2" and saves us from sending those coordinates again.

GX supports both 8-bit and 16-bit position indices:

| Position format | Reserved index | Meaning |
| --- | --- | --- |
| 8-bit | `0xff` (255) | Skip this vertex |
| 16-bit | `0xffff` (65'535) | Skip this vertex |

The skip rule applies to the indexed **position field**, while other fields use `ff` according to their own formats.

Nintendo's [US6700586B1 patent](https://patents.google.com/patent/US6700586B1/en) documents these reserved values.

## Skipping a vertex

If we send:

```text
0, 1, SKIP, 2, 3
```

the retained vertices are simply `0, 1, 2, 3`, giving us the same rectangle because the skipped record doesn't occupy a corner in the strip.

Select "Array entry" below to fetch the reserved index as point P and show the triangles touching it in red.

<VertexSkipDemo />

Move the marker to the end: fetching P adds one red triangle to the blue rectangle, just like Luigi's spike.

With the marker in the middle, the demo's restart option splits the strip into two pairs of vertices, neither of which can form a triangle. The dashed outline is just a guide.

## Back to Luigi

The affected Mario Golf strip contained 11 valid vertices followed by a skip record, but Gecko fetched a position for that final record anyway and interpreted unrelated memory as:

```text
[-27.166016, 15.875, 0.0]
```

Transforming that point and connecting it to the previous two corners produced the spike.

The fix removes skipped records before vertex decoding and updates the vertex count **for each draw**. Keeping draws separate prevents a removed record from accidentally joining one draw's vertices to the next.

Those corrected boundaries also feed the depth plane tracking used by [z-freeze](/posts/z-freeze/), since both features need to agree about which triangles exist.

## The bytes still exist

Skipping a vertex doesn't remove its bytes from the incoming command stream.

For example, an 8-bit position index followed by an **RGBA** color takes five bytes, even when the vertex is skipped. Here are three consecutive records:

<VertexRecords />

After reading the skip index `FF`, the parser must pass over all four color bytes or it will mistake `E6` for the next position index and lose its place. The alpha byte `FF` just means fully opaque.

A 16-bit position index takes two bytes, making the same record six bytes long, while other formats can add more attributes. The active format determines the position field and record boundaries.

The simplified logic looks like this:

```text
for each vertex record in the draw:
    record = consume the complete record using the current format

    if its indexed position is the reserved all ones value:
        continue

    decode the record and append its vertex

assemble the draw's triangles from the retained vertices
```

## Checking the behavior

My [test](https://github.com/ioncodes/gamecube-wii-tests/tree/master/vertex-skip) draws the same rectangle with direct positions, 8-bit indices and 16-bit indices, inserting a skip between vertices 1 and 2 in both indexed versions. All three should match:

<TestPanels kind="vertex-skip" />

The test stores a displaced position at the reserved array entries and makes the skipped records red, so an incorrect lookup produces obvious stray geometry. Build it with `make vertex-skip` and run `build/vertex-skip.dol`.

Gecko's implementation lives in [`filter_skipped_vertices`](https://github.com/ioncodes/gecko/blob/07a030f8d1aae494cac0e061c135ed0c75b45ff9/crates/gecko/src/flipper/gx/vertex.rs). All that trouble over a vertex that wasn't supposed to exist :)
